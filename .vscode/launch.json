{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug build_push_swap",
            "args": ["4", "24", "6", "0"],
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/push_swap",
            "cwd": "${workspaceFolder}/",
            "stopAtEntry": false,
            "preLaunchTask": "build_push_swap",
            "MIMode": "gdb"
        }
    ]
}
